var express = require('express')
var router = express.Router()

/* GET home page. */
router.get('/', function(req, res, next) {
  //gets the query for the main page
  //currently:no idea what will go here
  alert('thinking about thinking')
})

module.exports = router
